[package]
name = "renderer"
version = "0.1.0"
edition = "2024"

[dependencies]
wgpu = "29.0"
bytemuck = { version = "1.21", features = ["derive"] }
winit = { version = "0.30", features = ["rwh_06"] }
